take clone of project by running command

git clone git@github.com:Ajazsakib/hospital-api.git

Install Dependencies by run the commad

npm install

start the project by running

npm start

Note- Live url is not working
